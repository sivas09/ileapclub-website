services:
  - type: web
    name: ileap-member-portal-api
    env: node
    plan: free
    rootDir: member-portal
    buildCommand: npm install && npm run build && npm run prisma:migrate:deploy
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_ORIGIN
        value: https://memberportal.ileapclub.com
